@authenticated-user
Feature: 비활성화 워크로드 목록 페이지 진입
  As a 사용자,
  I want to 비활성화 탭 클릭 시
  So that 비활성화된 워크로드 목록을 조회할 수 있다

  Background:
    When 사용자가 비활성화 워크로드 목록 페이지로 진입한다
    And 워크스페이스가 선택되어 있다

  # ============================================
  # 기본 진입 및 공통 UI 검증
  # ============================================

  @TESTUC-32 @smoke
  Scenario: 비활성화 워크로드 목록 페이지 진입 시 기본 UI 표시
    Then 워크로드 목록 페이지가 표시된다
    And URL이 "/user/workload/disabled"와 일치한다
    And 네비게이션 메뉴 중 "워크로드" 메뉴가 활성화되어 있다
    And "비활성화" 탭이 선택되어 있다
    And 목록 테이블이 표시된다
    And 목록에 총 개수가 표시된다
    And 페이지네이션이 표시된다

  @TESTUC-32 @smoke
  Scenario: 필터 및 검색 UI 기본 상태 검증
    Then 잡 타입 필터가 빈 값으로 표시된다
    And 상태 필터가 빈 값으로 표시된다
    And 검색창이 빈 값으로 표시된다

  # ============================================
  # 데이터 유효성 검증
  # ============================================

  @TESTUC-32 @regression
  Scenario: 비활성화 워크로드 데이터 유효성 검증
    Then 각 워크로드의 이름이 빈 값이 아니다
    And 각 워크로드의 잡 타입이 다음 중 하나이다:
      | 잡 타입       |
      | batch         |
      | interactive   |
      | distributed   |
    And 각 워크로드의 상태가 다음 중 하나로 표시된다:
      | 상태      |
      | completed |

  # ============================================
  # 액션 버튼 활성화 상태 검증
  # ============================================

  @TESTUC-32 @regression
  Scenario: 비활성화 워크로드 액션 버튼 활성화 상태 검증
    Given 목록에 상태가 종료인 워크로드가 있다
    Then 워크로드 행의 로그 버튼이 활성화되어 있다
    And 워크로드 행의 모니터링 버튼이 활성화되어 있다
    And 워크로드 행의 삭제 버튼이 활성화되어 있다
    And 워크로드 행의 재시작 버튼이 활성화되어 있다

  # ============================================
  # 로그 조회
  # ============================================

  @TESTUC-36 @regression 
  Scenario: 비활성화 워크로드 로그 조회
    Given 목록에 상태가 종료인 워크로드가 있다
    And 워크로드 행의 로그 버튼이 활성화되어 있다
    When 워크로드 행의 로그 버튼을 클릭하여 로그 페이지로 이동한다
    Then URL이 "/user/workload/[id]/log?workspaceId="를 포함한다
    And 로그 영역에 설정된 테마가 적용되어 있다
    And 로그 영역에 하나 이상의 로그 라인이 존재한다
    And 로그 모니터링 버튼이 표시된다
    And 로그 테마 변경 버튼이 표시된다

  # ============================================
  # 모니터링 조회
  # ============================================

  @TESTUC-57 @regression 
  Scenario: 비활성화 워크로드 모니터링 조회
    Given 목록에 상태가 종료인 워크로드가 있다
    And 워크로드 행의 모니터링 버튼이 활성화되어 있다
    When 워크로드 행의 모니터링 버튼을 클릭하여 모니터링 페이지로 이동한다
    Then URL이 "/user/workload/[id]/monitoring?workspaceId="를 포함한다
    And 워크로드 CPU 사용량 차트가 표시된다
    And 워크로드 Memory 사용량 차트가 표시된다
    And 워크로드 GPU 사용률 차트가 표시된다
    And 워크로드 GPU 메모리 차트가 표시된다
