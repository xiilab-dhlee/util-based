/**
 * Generated by orval v7.17.2 🍺
 * Do not edit manually.
 * astrago-backend API Documentation
 * Astrago Backend V2 REST API 문서입니다.

## 인증 방법
1. Keycloak을 통해 JWT 토큰을 발급받습니다
2. 우측 상단 'Authorize' 버튼을 클릭합니다
3. JWT 토큰을 입력합니다 (Bearer 접두사 제외)
4. 'Authorize' 버튼을 클릭하여 인증을 완료합니다

## API 설계 원칙
- Hexagonal Architecture 기반 설계
- RESTful API 표준 준수
- HTTP 상태 코드 표준 준수 (201 Created, 204 No Content 등)
- 모든 요청/응답은 JSON 형식 사용

## 응답 형식
모든 API는 다음과 같은 공통 응답 형식을 사용합니다:
```json
{
  "data": { },
  "success": true,
  "message": "요청이 성공적으로 처리되었습니다"
}
```
 * OpenAPI spec version: 2.0.0
 */

import type {
  MutationFunction,
  QueryClient,
  UseMutationOptions,
  UseMutationResult,
} from "@tanstack/react-query";
import { useMutation } from "@tanstack/react-query";

import { customInstance } from "../../../shared/api/axios-mutator";
import type {
  BaseResponseUnit,
  PrometheusAlertPayload,
} from "../astragoBackendAPIDocumentation.schemas";

/**
 * Prometheus AlertManager에서 발송하는 Alert Webhook을 수신합니다. 인증 불필요.
 * @summary Prometheus Alert 수신
 */
export const receiveAlert = (
  prometheusAlertPayload: PrometheusAlertPayload,
  signal?: AbortSignal,
) => {
  return customInstance<BaseResponseUnit>({
    url: `/api/v1/monitoring-notifications/receive`,
    method: "POST",
    headers: { "Content-Type": "application/json" },
    data: prometheusAlertPayload,
    signal,
  });
};

export const getReceiveAlertMutationOptions = <
  TError = unknown,
  TContext = unknown,
>(options?: {
  mutation?: UseMutationOptions<
    Awaited<ReturnType<typeof receiveAlert>>,
    TError,
    { data: PrometheusAlertPayload },
    TContext
  >;
}): UseMutationOptions<
  Awaited<ReturnType<typeof receiveAlert>>,
  TError,
  { data: PrometheusAlertPayload },
  TContext
> => {
  const mutationKey = ["receiveAlert"];
  const { mutation: mutationOptions } = options
    ? options.mutation &&
      "mutationKey" in options.mutation &&
      options.mutation.mutationKey
      ? options
      : { ...options, mutation: { ...options.mutation, mutationKey } }
    : { mutation: { mutationKey } };

  const mutationFn: MutationFunction<
    Awaited<ReturnType<typeof receiveAlert>>,
    { data: PrometheusAlertPayload }
  > = (props) => {
    const { data } = props ?? {};

    return receiveAlert(data);
  };

  return { mutationFn, ...mutationOptions };
};

export type ReceiveAlertMutationResult = NonNullable<
  Awaited<ReturnType<typeof receiveAlert>>
>;
export type ReceiveAlertMutationBody = PrometheusAlertPayload;
export type ReceiveAlertMutationError = unknown;

/**
 * @summary Prometheus Alert 수신
 */
export const useReceiveAlert = <TError = unknown, TContext = unknown>(
  options?: {
    mutation?: UseMutationOptions<
      Awaited<ReturnType<typeof receiveAlert>>,
      TError,
      { data: PrometheusAlertPayload },
      TContext
    >;
  },
  queryClient?: QueryClient,
): UseMutationResult<
  Awaited<ReturnType<typeof receiveAlert>>,
  TError,
  { data: PrometheusAlertPayload },
  TContext
> => {
  const mutationOptions = getReceiveAlertMutationOptions(options);

  return useMutation(mutationOptions, queryClient);
};
