/**
 * Generated by orval v7.17.2 🍺
 * Do not edit manually.
 * astrago-backend API Documentation
 * Astrago Backend V2 REST API 문서입니다.

## 인증 방법
1. Keycloak을 통해 JWT 토큰을 발급받습니다
2. 우측 상단 'Authorize' 버튼을 클릭합니다
3. JWT 토큰을 입력합니다 (Bearer 접두사 제외)
4. 'Authorize' 버튼을 클릭하여 인증을 완료합니다

## API 설계 원칙
- Hexagonal Architecture 기반 설계
- RESTful API 표준 준수
- HTTP 상태 코드 표준 준수 (201 Created, 204 No Content 등)
- 모든 요청/응답은 JSON 형식 사용

## 응답 형식
모든 API는 다음과 같은 공통 응답 형식을 사용합니다:
```json
{
  "data": { },
  "success": true,
  "message": "요청이 성공적으로 처리되었습니다"
}
```
 * OpenAPI spec version: 2.0.0
 */
import * as zod from "zod";

/**
 * 특정 워크로드 잡 타입의 자원 회수 정책을 조회합니다. ADMIN 또는 SUPER_ADMIN 권한이 필요합니다.
 * @summary 자원 회수 정책 단건 조회
 */
export const getPolicyParams = zod.object({
  jobType: zod
    .enum(["INTERACTIVE", "BATCH", "DISTRIBUTED"])
    .describe("워크로드 잡 타입"),
});

export const getPolicyResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .object({
        workloadJobType: zod
          .enum(["INTERACTIVE", "BATCH", "DISTRIBUTED"])
          .describe("워크로드 잡 타입"),
        isEnabled: zod.boolean().describe("리소스 회수 활성화 여부"),
        operatingHour: zod.number().describe("회수 기준 시간(시간)"),
        metrics: zod
          .object({
            gpu: zod.number().describe("GPU 사용률 임계값(%)"),
            cpu: zod.number().describe("CPU 사용률 임계값(%)"),
            mem: zod.number().describe("메모리 사용률 임계값(%)"),
          })
          .strict()
          .describe("회수 기준 메트릭 응답"),
        reclaimOperator: zod.enum(["AND", "OR"]).describe("회수 조건 연산자"),
        reclaimWarningCount: zod.number().describe("회수 전 경고 횟수"),
      })
      .strict()
      .optional()
      .describe("자원 회수 정책 설정 응답"),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 자원 회수 정책을 수정합니다. 회수 기준 시간, 메트릭, 조건, 경고 횟수를 설정할 수 있습니다. ADMIN 또는 SUPER_ADMIN 권한이 필요합니다.
 * @summary 자원 회수 정책 수정
 */
export const updateReclaimPolicyParams = zod.object({
  jobType: zod
    .enum(["INTERACTIVE", "BATCH", "DISTRIBUTED"])
    .describe("워크로드 잡 타입"),
});

export const updateReclaimPolicyBodyOperatingHourMax = 24;

export const updateReclaimPolicyBodyMetricsGpuMin = 0;
export const updateReclaimPolicyBodyMetricsGpuMax = 100;

export const updateReclaimPolicyBodyMetricsCpuMin = 0;
export const updateReclaimPolicyBodyMetricsCpuMax = 100;

export const updateReclaimPolicyBodyMetricsMemMin = 0;
export const updateReclaimPolicyBodyMetricsMemMax = 100;

export const updateReclaimPolicyBodyReclaimWarningCountMin = 0;

export const updateReclaimPolicyBody = zod
  .object({
    operatingHour: zod
      .number()
      .min(1)
      .max(updateReclaimPolicyBodyOperatingHourMax)
      .describe("회수 기준 시간(시간)"),
    metrics: zod
      .object({
        gpu: zod
          .number()
          .min(updateReclaimPolicyBodyMetricsGpuMin)
          .max(updateReclaimPolicyBodyMetricsGpuMax)
          .describe("GPU 사용률 임계값(%)"),
        cpu: zod
          .number()
          .min(updateReclaimPolicyBodyMetricsCpuMin)
          .max(updateReclaimPolicyBodyMetricsCpuMax)
          .describe("CPU 사용률 임계값(%)"),
        mem: zod
          .number()
          .min(updateReclaimPolicyBodyMetricsMemMin)
          .max(updateReclaimPolicyBodyMetricsMemMax)
          .describe("메모리 사용률 임계값(%)"),
      })
      .strict()
      .describe("회수 기준 메트릭"),
    reclaimOperator: zod.enum(["AND", "OR"]).describe("회수 조건 연산자"),
    reclaimWarningCount: zod
      .number()
      .min(updateReclaimPolicyBodyReclaimWarningCountMin)
      .describe("회수 전 경고 횟수"),
  })
  .strict()
  .describe("자원 회수 정책 수정 요청");

export const updateReclaimPolicyResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 자원 회수 정책을 실행 또는 종료합니다. 활성화 여부를 on/off 할 수 있습니다. ADMIN 또는 SUPER_ADMIN 권한이 필요합니다.
 * @summary 자원 회수 정책 실행/종료
 */
export const updateReclaimPolicyEnabledParams = zod.object({
  jobType: zod
    .enum(["INTERACTIVE", "BATCH", "DISTRIBUTED"])
    .describe("워크로드 잡 타입"),
});

export const updateReclaimPolicyEnabledBody = zod
  .object({
    isEnabled: zod.boolean().describe("활성화 여부"),
  })
  .strict()
  .describe("자원 회수 정책 실행/종료 요청");

export const updateReclaimPolicyEnabledResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 모든 워크로드 잡 타입의 자원 회수 정책을 조회합니다. ADMIN 또는 SUPER_ADMIN 권한이 필요합니다.
 * @summary 자원 회수 정책 전체 목록 조회
 */
export const getAllPoliciesResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .array(
        zod
          .object({
            workloadJobType: zod
              .enum(["INTERACTIVE", "BATCH", "DISTRIBUTED"])
              .describe("워크로드 잡 타입"),
            isEnabled: zod.boolean().describe("리소스 회수 활성화 여부"),
            operatingHour: zod.number().describe("회수 기준 시간(시간)"),
            metrics: zod
              .object({
                gpu: zod.number().describe("GPU 사용률 임계값(%)"),
                cpu: zod.number().describe("CPU 사용률 임계값(%)"),
                mem: zod.number().describe("메모리 사용률 임계값(%)"),
              })
              .strict()
              .describe("회수 기준 메트릭 응답"),
            reclaimOperator: zod
              .enum(["AND", "OR"])
              .describe("회수 조건 연산자"),
            reclaimWarningCount: zod.number().describe("회수 전 경고 횟수"),
          })
          .strict()
          .describe("자원 회수 정책 설정 응답"),
      )
      .optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 자원 회수 스캔 히스토리 목록을 페이지네이션으로 조회합니다. ADMIN 또는 SUPER_ADMIN 권한이 필요합니다.
 * @summary 자원 회수 스캔 히스토리 목록 조회
 */
export const getScanHistoryListQueryPageRequestPageSizeMax = 100;

export const getScanHistoryListQueryParams = zod.object({
  pageRequest: zod.object({
    pageNo: zod.number().describe("페이지 번호 (0부터 시작)"),
    pageSize: zod
      .number()
      .min(1)
      .max(getScanHistoryListQueryPageRequestPageSizeMax)
      .describe("페이지 크기"),
  }),
});

export const getScanHistoryListResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .object({
        totalSize: zod.number(),
        totalPageNum: zod.number(),
        currentPageNo: zod.number(),
        content: zod.array(
          zod
            .object({
              scanHistoryId: zod.number().describe("스캔 히스토리 ID"),
              reclaimWarningWorkloadCount: zod
                .number()
                .describe("회수 경고 워크로드 수"),
              reclaimScanWorkloadCount: zod
                .number()
                .describe("검사 대상 워크로드 수"),
              reclaimedWorkloadCount: zod
                .number()
                .describe("회수된 워크로드 수"),
              reclaimedGpuCount: zod.number().describe("회수된 GPU 수"),
              reclaimedCpuCore: zod.number().describe("회수된 CPU 코어 수"),
              reclaimedMemoryByte: zod.number().describe("회수된 메모리(byte)"),
              createdAt: zod.string().datetime({}).describe("스캔 실행 일시"),
            })
            .strict()
            .describe("자원 회수 스캔 히스토리 응답"),
        ),
      })
      .strict()
      .optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 특정 스캔 히스토리에 대한 워크로드별 스캔 결과 목록을 페이지네이션으로 조회합니다. ADMIN 또는 SUPER_ADMIN 권한이 필요합니다.
 * @summary 자원 회수 스캔 결과 목록 조회
 */
export const getScanResultListParams = zod.object({
  scanHistoryId: zod.number().describe("스캔 히스토리 ID"),
});

export const getScanResultListQueryPageRequestPageSizeMax = 100;

export const getScanResultListQueryParams = zod.object({
  pageRequest: zod.object({
    pageNo: zod.number().describe("페이지 번호 (0부터 시작)"),
    pageSize: zod
      .number()
      .min(1)
      .max(getScanResultListQueryPageRequestPageSizeMax)
      .describe("페이지 크기"),
  }),
});

export const getScanResultListResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .object({
        totalSize: zod.number(),
        totalPageNum: zod.number(),
        currentPageNo: zod.number(),
        content: zod.array(
          zod
            .object({
              scanResultId: zod.number().describe("스캔 결과 ID"),
              workloadId: zod.number().describe("워크로드 ID"),
              scanHistoryId: zod.number().describe("스캔 히스토리 ID"),
              createdAt: zod
                .string()
                .datetime({})
                .describe("스캔 결과 생성 일시"),
            })
            .strict()
            .describe("자원 회수 스캔 결과 응답"),
        ),
      })
      .strict()
      .optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();
