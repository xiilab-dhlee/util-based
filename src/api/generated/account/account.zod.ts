/**
 * Generated by orval v7.17.2 🍺
 * Do not edit manually.
 * astrago-backend API Documentation
 * Astrago Backend V2 REST API 문서입니다.

## 인증 방법
1. Keycloak을 통해 JWT 토큰을 발급받습니다
2. 우측 상단 'Authorize' 버튼을 클릭합니다
3. JWT 토큰을 입력합니다 (Bearer 접두사 제외)
4. 'Authorize' 버튼을 클릭하여 인증을 완료합니다

## API 설계 원칙
- Hexagonal Architecture 기반 설계
- RESTful API 표준 준수
- HTTP 상태 코드 표준 준수 (201 Created, 204 No Content 등)
- 모든 요청/응답은 JSON 형식 사용

## 응답 형식
모든 API는 다음과 같은 공통 응답 형식을 사용합니다:
```json
{
  "data": { },
  "success": true,
  "message": "요청이 성공적으로 처리되었습니다"
}
```
 * OpenAPI spec version: 2.0.0
 */
import * as zod from "zod";

/**
 * 특정 워크스페이스에 대한 특정 알림 설정의 채널별 수신 여부를 수정합니다. 본인의 알림 설정만 수정할 수 있습니다.
 * @summary 워크스페이스별 알림 설정 수정
 */
export const updateNotificationSetParams = zod.object({
  accountId: zod.string().describe("계정 ID (Keycloak User ID, UUID 형식)"),
  workspaceId: zod.number().describe("워크스페이스 ID"),
  notificationSetId: zod.number().describe("알림 설정 ID"),
});

export const updateNotificationSetBody = zod
  .object({
    isSystemNotificationEnabled: zod
      .boolean()
      .describe("시스템 알림 활성화 여부"),
    isEmailNotificationEnabled: zod
      .boolean()
      .describe("이메일 알림 활성화 여부"),
  })
  .strict()
  .describe(
    "알림 설정 수정 요청. 특정 알림 설정에 대한 채널별 수신 여부를 수정합니다. 알림 설정 ID는 URL Path Variable로 전달됩니다.",
  );

export const updateNotificationSetResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 사용자의 프로필 정보를 조회합니다. 본인의 프로필만 조회할 수 있습니다.
 * @summary 프로필 조회
 */
export const getProfileParams = zod.object({
  accountId: zod.string().describe("계정 ID (Keycloak User ID, UUID 형식)"),
});

export const getProfileResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    data: zod
      .object({
        accountName: zod.string().describe("계정 표시 이름 (성 + 이름)"),
        accountId: zod
          .string()
          .describe("계정 고유 ID (Keycloak User ID, UUID 형식)"),
        email: zod.string().describe("이메일 주소"),
        accountRole: zod
          .string()
          .describe("계정 역할. 가능한 값: SUPER_ADMIN, ADMIN, USER"),
        groupName: zod
          .array(zod.string())
          .describe("사용자가 속한 그룹 이름 목록"),
        createdAt: zod
          .string()
          .datetime({})
          .optional()
          .describe("계정 생성 일시 (ZonedDateTime, ISO 8601 형식)"),
        isEnabled: zod
          .boolean()
          .describe("계정 활성화 여부. true: 활성화, false: 비활성화"),
        workspaceCount: zod
          .number()
          .describe("사용자가 소속된 워크스페이스 개수"),
        workspaceLimitCount: zod
          .number()
          .describe("사용자가 생성 가능한 워크스페이스 최대 개수"),
      })
      .strict()
      .optional()
      .describe("사용자 프로필 조회 응답"),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 사용자의 프로필 정보를 수정합니다. 본인의 프로필만 수정할 수 있습니다.
 * @summary 프로필 수정
 */
export const updateProfileParams = zod.object({
  accountId: zod.string().describe("계정 ID (Keycloak User ID, UUID 형식)"),
});

export const updateProfileBodyFirstNameMax = 25;

export const updateProfileBodyLastNameMax = 25;

export const updateProfileBodyNewPasswordMin = 8;
export const updateProfileBodyNewPasswordMax = 16;

export const updateProfileBodyNewPasswordRegExp =
  /^(?=.*[A-Za-z])(?=.*\d)(?=.*[!@#$%^&()])[A-Za-z0-9!@#$%^&()]{8,16}$/;

export const updateProfileBody = zod
  .object({
    firstName: zod
      .string()
      .min(1)
      .max(updateProfileBodyFirstNameMax)
      .optional()
      .describe(
        "이름(필수). lastName과 함께 제공되어야 합니다. null인 경우 이름을 변경하지 않습니다. 최대 25자, 한글/영문/숫자/-, _, ., 공백 허용",
      ),
    lastName: zod
      .string()
      .min(1)
      .max(updateProfileBodyLastNameMax)
      .optional()
      .describe(
        "성(필수). firstName과 함께 제공되어야 합니다. null인 경우(=초기 admin) 이름을 변경하지 않습니다. 최대 25자, 한글/영문/숫자/-, _, ., 공백 허용",
      ),
    newPassword: zod
      .string()
      .min(updateProfileBodyNewPasswordMin)
      .max(updateProfileBodyNewPasswordMax)
      .regex(updateProfileBodyNewPasswordRegExp)
      .optional()
      .describe(
        "새 비밀번호 (선택적). null인 경우(=초기 admin) 비밀번호를 변경하지 않습니다. 규칙: 8~16자, 영문(대소문자), 숫자, 특수문자(!@#$%^&()) 조합",
      ),
    isValid: zod.boolean(),
  })
  .strict()
  .describe(
    "프로필 수정 요청. firstName/lastName 쌍 또는 newPassword 중 최소 하나는 반드시 존재해야 합니다. firstName과 lastName은 함께 제공되어야 합니다.",
  );

export const updateProfileResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 특정 알림을 읽음 상태로 변경합니다. 본인의 알림만 읽음 처리할 수 있습니다.
 * @summary 알림 읽음 처리
 */
export const markNotificationAsReadParams = zod.object({
  accountId: zod.string().describe("계정 ID (Keycloak User ID, UUID 형식)"),
  notificationId: zod.number().describe("알림 ID"),
});

export const markNotificationAsReadResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 인증 코드를 검증하고 새 비밀번호로 변경합니다. 보안상 유효하지 않은 이메일, 잘못된 코드, 만료된 코드 모두 동일한 에러를 반환합니다. 성공 시 인증 코드는 자동으로 삭제됩니다.
 * @summary 비밀번호 재설정
 */

export const resetPasswordBodyCodeRegExp = /^[0-9]{6}$/;
export const resetPasswordBodyPasswordMin = 8;
export const resetPasswordBodyPasswordMax = 16;

export const resetPasswordBodyPasswordRegExp =
  /^(?=.*[A-Za-z])(?=.*\d)(?=.*[!@#$%^&()])[A-Za-z0-9!@#$%^&()]{8,16}$/;

export const resetPasswordBody = zod
  .object({
    email: zod.string().email().min(1).describe("이메일 주소"),
    code: zod
      .string()
      .min(1)
      .regex(resetPasswordBodyCodeRegExp)
      .describe("인증 코드 (6자리 숫자)"),
    password: zod
      .string()
      .min(resetPasswordBodyPasswordMin)
      .max(resetPasswordBodyPasswordMax)
      .regex(resetPasswordBodyPasswordRegExp)
      .describe(
        "새 비밀번호. 규칙: 8~16자, 영문(대소문자), 숫자, 특수문자(!@#$%^&()) 조합",
      ),
  })
  .strict()
  .describe("비밀번호 재설정 (새 비밀번호 설정) 요청");

export const resetPasswordResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 특정 워크스페이스를 핀 고정합니다. 본인의 계정에만 핀을 저장할 수 있습니다.
 * @summary 워크스페이스 핀 저장
 */
export const pinWorkspaceParams = zod.object({
  accountId: zod.string().describe("계정 ID (Keycloak User ID, UUID 형식)"),
  workspaceId: zod.number().describe("워크스페이스 ID"),
});

/**
 * 특정 워크스페이스의 핀을 해제합니다. 본인의 계정에서만 핀을 해제할 수 있습니다.
 * @summary 워크스페이스 핀 해제
 */
export const unpinWorkspaceParams = zod.object({
  accountId: zod.string().describe("계정 ID (Keycloak User ID, UUID 형식)"),
  workspaceId: zod.number().describe("워크스페이스 ID"),
});

/**
 * 새로운 사용자 계정을 생성합니다. 가입 승인 모드가 활성화된 경우 관리자 승인 후 로그인이 가능합니다.
 * @summary 회원가입
 */

export const signupBodyPasswordMin = 8;
export const signupBodyPasswordMax = 16;

export const signupBodyPasswordRegExp =
  /^(?=.*[A-Za-z])(?=.*\d)(?=.*[!@#$%^&()])[A-Za-z0-9!@#$%^&()]{8,16}$/;
export const signupBodyFirstNameMax = 25;

export const signupBodyLastNameMax = 25;

export const signupBody = zod
  .object({
    email: zod.string().email().min(1).describe("이메일 주소"),
    password: zod
      .string()
      .min(signupBodyPasswordMin)
      .max(signupBodyPasswordMax)
      .regex(signupBodyPasswordRegExp)
      .describe(
        "비밀번호. 규칙: 8~16자, 영문(대소문자), 숫자, 특수문자(!@#$%^&()) 조합",
      ),
    firstName: zod
      .string()
      .min(1)
      .max(signupBodyFirstNameMax)
      .describe("이름 (First Name). 최소 1자, 최대 25자"),
    lastName: zod
      .string()
      .min(1)
      .max(signupBodyLastNameMax)
      .describe("성 (Last Name). 최소 1자, 최대 25자"),
    groupId: zod
      .array(zod.string())
      .optional()
      .describe(
        "그룹 ID 목록. 회원 가입 시 배정할 그룹들의 ID 목록입니다. 여러 그룹에 동시 배정이 가능합니다.",
      ),
    accountRole: zod
      .enum(["SUPER_ADMIN", "ADMIN", "USER"])
      .describe("계정 역할. 허용값: SUPER_ADMIN, ADMIN, USER"),
  })
  .strict()
  .describe("회원가입 요청");

/**
 * 사용자의 이메일로 인증 코드를 발송합니다. Rate limiting이 적용되어 1분 이내 재요청을 방지합니다. 재발송 시에도 동일한 API를 사용하며, 기존 코드는 새 코드로 자동 교체됩니다.
 * @summary 비밀번호 재설정 인증코드 발송
 */

export const requestPasswordResetBody = zod
  .object({
    email: zod.string().email().min(1).describe("이메일 주소"),
  })
  .strict()
  .describe("비밀번호 재설정 인증코드 발송 요청");

/**
 * 사용자가 입력한 인증 코드가 유효한지 검증합니다. 클라이언트에서 코드 입력 후 즉시 피드백을 제공하기 위해 사용됩니다. 실제 비밀번호 변경은 /password/reset API에서 수행됩니다.
 * @summary 비밀번호 재설정 인증코드 검증
 */

export const verifyPasswordResetCodeBodyCodeMin = 6;
export const verifyPasswordResetCodeBodyCodeMax = 6;

export const verifyPasswordResetCodeBody = zod
  .object({
    email: zod.string().email().min(1).describe("이메일 주소"),
    code: zod
      .string()
      .min(verifyPasswordResetCodeBodyCodeMin)
      .max(verifyPasswordResetCodeBodyCodeMax)
      .describe("인증 코드 (6자리 숫자)"),
  })
  .strict()
  .describe("비밀번호 재설정 인증코드 검증 요청");

export const verifyPasswordResetCodeResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 특정 워크스페이스에 대한 알림 설정 목록을 조회합니다. 계정 권한에 따라 조회 가능한 알림 설정이 결정됩니다. 본인의 알림 설정만 조회할 수 있습니다.
 * @summary 워크스페이스별 알림 설정 조회
 */
export const getNotificationSetsParams = zod.object({
  accountId: zod.string().describe("계정 ID (Keycloak User ID, UUID 형식)"),
  workspaceId: zod.number().describe("워크스페이스 ID"),
});

export const getNotificationSetsResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    data: zod
      .array(
        zod
          .object({
            notificationSetId: zod.number().describe("알림 설정 고유 ID"),
            notificationSetName: zod
              .string()
              .describe("알림 설정 표시 이름 (UI용)"),
            isSystemNotificationEnabled: zod
              .boolean()
              .describe("시스템 내 알림 수신 활성화 여부"),
            isEmailNotificationEnabled: zod
              .boolean()
              .describe("이메일 알림 수신 활성화 여부"),
            notificationType: zod
              .string()
              .describe(
                "알림 타입. 가능한 값: WORKSPACE_INVITE, WORKSPACE_ROLE_CHANGE, SIGNUP_REQUEST 등",
              ),
            notificationRole: zod
              .string()
              .describe("알림 대상 역할. 가능한 값: USER, ADMIN"),
          })
          .strict()
          .describe("알림 설정 조회 응답 (사용자용)"),
      )
      .optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 사용자의 알림 목록을 페이징하여 조회합니다. 본인의 알림만 조회할 수 있습니다.
 * @summary 알림 목록 조회
 */
export const getNotificationsParams = zod.object({
  accountId: zod.string().describe("계정 ID (Keycloak User ID, UUID 형식)"),
});

export const getNotificationsQueryPageableRequestPageSizeMax = 100;

export const getNotificationsQueryParams = zod.object({
  pageableRequest: zod.object({
    pageNo: zod.number().describe("페이지 번호 (0부터 시작)"),
    pageSize: zod
      .number()
      .min(1)
      .max(getNotificationsQueryPageableRequestPageSizeMax)
      .describe("페이지 크기"),
  }),
  filterRequest: zod.object({
    isRead: zod
      .boolean()
      .optional()
      .describe(
        "읽음 여부 필터. true: 읽은 알림만, false: 읽지 않은 알림만, null: 전체",
      ),
    notificationType: zod
      .enum([
        "LICENSE",
        "ACCOUNT",
        "VULNERABILITY",
        "NODE",
        "WORKSPACE",
        "WORKLOAD",
        "MONITORING",
      ])
      .optional()
      .describe(
        "알림 타입 필터. 가능한 값: WORKSPACE_INVITE, WORKSPACE_ROLE_CHANGE, SIGNUP_REQUEST 등. null: 전체",
      ),
    notificationRole: zod
      .enum(["SUPER_ADMIN", "ADMIN", "USER", "ALL"])
      .optional()
      .describe("알림 대상 역할 필터. 가능한 값: USER, ADMIN. null: 전체"),
  }),
});

export const getNotificationsResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    data: zod
      .object({
        totalSize: zod.number(),
        totalPageNum: zod.number(),
        currentPageNo: zod.number(),
        content: zod.array(
          zod
            .object({
              notificationId: zod.number().describe("알림 고유 ID"),
              notificationContent: zod.string().describe("알림 본문 메시지"),
              notificationType: zod
                .string()
                .describe(
                  "알림 타입. 가능한 값: WORKSPACE_INVITE, WORKSPACE_ROLE_CHANGE, SIGNUP_REQUEST 등",
                ),
              createDateTime: zod
                .string()
                .datetime({})
                .describe("알림 생성 일시 (ZonedDateTime)"),
              isRead: zod.boolean().describe("사용자 읽음 여부"),
            })
            .strict()
            .describe("알림 목록 조회 응답 (단일 알림 정보)"),
        ),
      })
      .strict()
      .optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();
