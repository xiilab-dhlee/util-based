/**
 * Generated by orval v7.17.2 🍺
 * Do not edit manually.
 * astrago-backend API Documentation
 * Astrago Backend V2 REST API 문서입니다.

## 인증 방법
1. Keycloak을 통해 JWT 토큰을 발급받습니다
2. 우측 상단 'Authorize' 버튼을 클릭합니다
3. JWT 토큰을 입력합니다 (Bearer 접두사 제외)
4. 'Authorize' 버튼을 클릭하여 인증을 완료합니다

## API 설계 원칙
- Hexagonal Architecture 기반 설계
- RESTful API 표준 준수
- HTTP 상태 코드 표준 준수 (201 Created, 204 No Content 등)
- 모든 요청/응답은 JSON 형식 사용

## 응답 형식
모든 API는 다음과 같은 공통 응답 형식을 사용합니다:
```json
{
  "data": { },
  "success": true,
  "message": "요청이 성공적으로 처리되었습니다"
}
```
 * OpenAPI spec version: 2.0.0
 */
import * as zod from "zod";

/**
 * 관리자가 이미지 태그 사용 요청을 반려합니다.
 * @summary 이미지 태그 사용 요청 반려
 */
export const rejectUsageRequestParams = zod.object({
  usageRequestId: zod.number().describe("이미지 태그 사용 요청 ID"),
});

export const rejectUsageRequestBodyRejectReasonMax = 2000;

export const rejectUsageRequestBody = zod
  .object({
    rejectReason: zod
      .string()
      .min(1)
      .max(rejectUsageRequestBodyRejectReasonMax)
      .describe("반려 사유 (필수, 최대 2000자)"),
  })
  .strict()
  .describe("이미지 태그 사용 요청 반려");

export const rejectUsageRequestResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 관리자가 이미지 태그 사용 요청을 승인합니다.
 * @summary 이미지 태그 사용 요청 승인
 */
export const approveUsageRequestParams = zod.object({
  usageRequestId: zod.number().describe("이미지 태그 사용 요청 ID"),
});

export const approveUsageRequestBodyApprovalReasonMax = 2000;

export const approveUsageRequestBody = zod
  .object({
    approvalReason: zod
      .string()
      .max(approveUsageRequestBodyApprovalReasonMax)
      .optional()
      .describe("승인 사유 (선택, 최대 2000자)"),
  })
  .strict()
  .describe("이미지 태그 사용 요청 승인");

export const approveUsageRequestResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 
            레지스트리의 이미지 태그 사용 요청 목록을 조회합니다.
            - registryType으로 공용/개인 레지스트리를 구분합니다.
            - 요청자, 이미지명, 태그명, 취약점 정보, 승인 상태 등을 제공합니다.
            - 승인 상태, 워크스페이스로 필터링하거나 키워드(이미지명/태그명)로 검색할 수 있습니다.
            - 이미지명, 워크스페이스명, 태그명, 보안검사결과, 요청자명, 요청일시로 정렬 가능합니다.
        
 * @summary 이미지 태그 사용 요청 목록 조회
 */
export const getUsageRequestListQueryPageNoMin = 0;

export const getUsageRequestListQueryPageSizeMax = 100;

export const getUsageRequestListQueryParams = zod.object({
  pageNo: zod
    .number()
    .min(getUsageRequestListQueryPageNoMin)
    .optional()
    .describe("페이지 번호 (0부터 시작)"),
  pageSize: zod
    .number()
    .min(1)
    .max(getUsageRequestListQueryPageSizeMax)
    .optional()
    .describe("페이지 크기"),
  keyword: zod.string().optional().describe("검색 키워드"),
  imageType: zod
    .enum(["BUILT_IN", "HUB", "PRIVATE", "PUBLIC"])
    .optional()
    .describe("이미지 타입 (PUBLIC/PRIVATE, 미지정 시 전체)"),
  approvalStatus: zod
    .enum(["APPROVAL_WAITING", "APPROVED", "REJECTED"])
    .optional()
    .describe("승인 상태 필터"),
  workspaceId: zod.number().optional().describe("워크스페이스 ID 필터"),
  sort: zod
    .enum([
      "IMAGE_NAME",
      "WORKSPACE_NAME",
      "IMAGE_TAG_NAME",
      "SECURITY_SCAN_RESULT",
      "CREATOR_NAME",
      "REQUESTED_AT",
    ])
    .optional()
    .describe("정렬 기준 필드"),
  order: zod.enum(["ASC", "DESC"]).optional().describe("정렬 순서"),
});

export const getUsageRequestListResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .object({
        totalSize: zod.number(),
        totalPageNum: zod.number(),
        currentPageNo: zod.number(),
        content: zod.array(
          zod
            .object({
              usageRequestId: zod.number().describe("사용 요청 ID"),
              imageTagId: zod.number().describe("이미지 태그 ID"),
              harborTagId: zod
                .number()
                .describe("Harbor 태그 ID (삭제 시 사용)"),
              imageTagName: zod.string().describe("이미지 태그명"),
              harborImageName: zod.string().describe("Harbor 이미지명"),
              imageDisplayName: zod
                .string()
                .optional()
                .describe("이미지 표시명"),
              requestReason: zod.string().describe("요청 사유"),
              decisionReason: zod
                .string()
                .optional()
                .describe("승인/반려 사유"),
              imageSizeByte: zod
                .number()
                .optional()
                .describe("이미지 크기 (bytes)"),
              workspaceId: zod.number().optional().describe("워크스페이스 ID"),
              workspaceName: zod.string().optional().describe("워크스페이스명"),
              creatorId: zod.string().describe("요청자 ID"),
              creatorName: zod.string().optional().describe("요청자명"),
              requestedAt: zod.string().datetime({}).describe("요청 일시"),
              approvalStatus: zod
                .enum(["APPROVAL_WAITING", "APPROVED", "REJECTED"])
                .describe("승인 상태"),
              deciderId: zod.string().optional().describe("결정자 ID"),
              deciderName: zod.string().optional().describe("결정자명"),
              decidedAt: zod
                .string()
                .datetime({})
                .optional()
                .describe("결정 일시"),
              vulnerability: zod
                .object({
                  criticalCount: zod.number().describe("치명적 취약점 수"),
                  highCount: zod.number().describe("높음 취약점 수"),
                  mediumCount: zod.number().describe("중간 취약점 수"),
                  lowCount: zod.number().describe("낮음 취약점 수"),
                  totalCount: zod.number().describe("전체 취약점 수"),
                })
                .strict()
                .optional()
                .describe("취약점 정보"),
            })
            .strict()
            .describe("이미지 태그 사용 요청 응답"),
        ),
      })
      .strict()
      .optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();
