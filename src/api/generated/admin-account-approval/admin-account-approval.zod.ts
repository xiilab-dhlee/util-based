/**
 * Generated by orval v7.17.2 🍺
 * Do not edit manually.
 * astrago-backend API Documentation
 * Astrago Backend V2 REST API 문서입니다.

## 인증 방법
1. Keycloak을 통해 JWT 토큰을 발급받습니다
2. 우측 상단 'Authorize' 버튼을 클릭합니다
3. JWT 토큰을 입력합니다 (Bearer 접두사 제외)
4. 'Authorize' 버튼을 클릭하여 인증을 완료합니다

## API 설계 원칙
- Hexagonal Architecture 기반 설계
- RESTful API 표준 준수
- HTTP 상태 코드 표준 준수 (201 Created, 204 No Content 등)
- 모든 요청/응답은 JSON 형식 사용

## 응답 형식
모든 API는 다음과 같은 공통 응답 형식을 사용합니다:
```json
{
  "data": { },
  "success": true,
  "message": "요청이 성공적으로 처리되었습니다"
}
```
 * OpenAPI spec version: 2.0.0
 */
import * as zod from "zod";

/**
 * 선택한 가입 요청을 일괄 반려합니다. 반려된 요청은 목록에서 제거됩니다. ADMIN 또는 SUPER_ADMIN 권한이 필요합니다.
 * @summary 가입 요청 반려
 */
export const rejectSignupRequestsBodyAccountIdMax = 2147483647;

export const rejectSignupRequestsBody = zod
  .object({
    accountId: zod
      .array(zod.string())
      .min(1)
      .max(rejectSignupRequestsBodyAccountIdMax)
      .describe(
        "반려할 계정 ID 목록 (Keycloak User ID 배열). 최소 1개 이상의 계정 ID가 필요합니다.",
      ),
  })
  .strict()
  .describe(
    "가입반려 요청 (관리자 전용). 관리자가 여러 계정을 한번에 반려할 수 있습니다.",
  );

export const rejectSignupRequestsResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .object({
        totalRequested: zod.number().describe("승인 요청한 총 계정 개수"),
        successCount: zod.number().describe("승인 성공한 계정 개수"),
        failureCount: zod.number().describe("승인 실패한 계정 개수"),
        failures: zod
          .array(
            zod
              .object({
                accountId: zod
                  .string()
                  .describe(
                    "실패한 계정 고유 ID (Keycloak User ID, UUID 형식)",
                  ),
                email: zod.string().describe("실패한 계정의 이메일 주소"),
                reason: zod.string().describe("승인 실패 사유 메시지"),
              })
              .strict()
              .describe("가입승인 실패 상세 정보"),
          )
          .describe("승인 실패한 계정 상세 목록 (실패가 없으면 빈 리스트)"),
      })
      .strict()
      .optional()
      .describe("가입승인 처리 결과 응답"),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 선택한 가입 요청을 일괄 승인합니다. 승인된 계정은 활성화되어 로그인이 가능해집니다. ADMIN 또는 SUPER_ADMIN 권한이 필요합니다.
 * @summary 가입 요청 승인
 */
export const approveSignupRequestsBodyAccountIdMax = 2147483647;

export const approveSignupRequestsBody = zod
  .object({
    accountId: zod
      .array(zod.string())
      .min(1)
      .max(approveSignupRequestsBodyAccountIdMax)
      .describe(
        "승인할 계정 ID 목록 (Keycloak User ID 배열). 최소 1개 이상의 계정 ID가 필요합니다.",
      ),
  })
  .strict()
  .describe(
    "가입승인 요청 (관리자 전용). 관리자가 여러 계정을 한번에 승인할 수 있습니다.",
  );

export const approveSignupRequestsResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .object({
        totalRequested: zod.number().describe("승인 요청한 총 계정 개수"),
        successCount: zod.number().describe("승인 성공한 계정 개수"),
        failureCount: zod.number().describe("승인 실패한 계정 개수"),
        failures: zod
          .array(
            zod
              .object({
                accountId: zod
                  .string()
                  .describe(
                    "실패한 계정 고유 ID (Keycloak User ID, UUID 형식)",
                  ),
                email: zod.string().describe("실패한 계정의 이메일 주소"),
                reason: zod.string().describe("승인 실패 사유 메시지"),
              })
              .strict()
              .describe("가입승인 실패 상세 정보"),
          )
          .describe("승인 실패한 계정 상세 목록 (실패가 없으면 빈 리스트)"),
      })
      .strict()
      .optional()
      .describe("가입승인 처리 결과 응답"),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 가입 승인 대기 중인 계정 목록을 페이지네이션으로 조회합니다. 키워드 검색 및 정렬 옵션을 지원합니다. ADMIN 또는 SUPER_ADMIN 권한이 필요합니다.
 * @summary 가입 승인 대기 목록 조회
 */
export const getSignupRequestsQueryPageSearchRequestPageSizeMax = 100;

export const getSignupRequestsQueryParams = zod.object({
  pageSearchRequest: zod.object({
    pageNo: zod.number().describe("페이지 번호 (0부터 시작)"),
    pageSize: zod
      .number()
      .min(1)
      .max(getSignupRequestsQueryPageSearchRequestPageSizeMax)
      .describe("페이지 크기"),
    keyword: zod.string().optional().describe("검색 키워드"),
  }),
  sortRequest: zod.object({
    sort: zod
      .enum(["ACCOUNT_NAME", "CREATED_AT"])
      .describe("정렬 필드: ACCOUNT_NAME, CREATED_AT. 미입력 시 ACCOUNT_NAME"),
    order: zod.enum(["ASC", "DESC"]).describe("정렬 순서"),
  }),
});

export const getSignupRequestsResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .object({
        totalSize: zod.number(),
        totalPageNum: zod.number(),
        currentPageNo: zod.number(),
        content: zod.array(
          zod
            .object({
              accountId: zod
                .string()
                .describe("계정 고유 ID (Keycloak User ID, UUID 형식)"),
              accountName: zod.string().describe("계정 표시 이름 (성 + 이름)"),
              email: zod.string().describe("이메일 주소"),
              createdAt: zod
                .string()
                .datetime({})
                .optional()
                .describe("가입 요청 일시 (ZonedDateTime, ISO 8601 형식)"),
            })
            .strict()
            .describe("가입승인 대기 목록 조회 응답 DTO (단일 계정 정보)"),
        ),
      })
      .strict()
      .optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();
