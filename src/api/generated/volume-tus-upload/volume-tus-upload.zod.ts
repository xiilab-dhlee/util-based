/**
 * Generated by orval v7.17.2 🍺
 * Do not edit manually.
 * astrago-backend API Documentation
 * Astrago Backend V2 REST API 문서입니다.

## 인증 방법
1. Keycloak을 통해 JWT 토큰을 발급받습니다
2. 우측 상단 'Authorize' 버튼을 클릭합니다
3. JWT 토큰을 입력합니다 (Bearer 접두사 제외)
4. 'Authorize' 버튼을 클릭하여 인증을 완료합니다

## API 설계 원칙
- Hexagonal Architecture 기반 설계
- RESTful API 표준 준수
- HTTP 상태 코드 표준 준수 (201 Created, 204 No Content 등)
- 모든 요청/응답은 JSON 형식 사용

## 응답 형식
모든 API는 다음과 같은 공통 응답 형식을 사용합니다:
```json
{
  "data": { },
  "success": true,
  "message": "요청이 성공적으로 처리되었습니다"
}
```
 * OpenAPI spec version: 2.0.0
 */
import * as zod from "zod";

/**
 * 
        새로운 파일 업로드를 생성합니다.
        업로드 생성 후 반환되는 uploadId를 사용하여 청크 업로드를 진행합니다.

        **필수 헤더:**
        - Tus-Resumable: 1.0.0
        - Upload-Length: 업로드할 파일의 전체 크기 (바이트)

        **선택 헤더:**
        - Upload-Metadata: Base64 인코딩된 메타데이터 (filename, filetype 등)

        **응답:**
        - 201 Created
        - Location 헤더: 업로드 ID (청크 업로드에 사용)

        **권한:** SUPER_ADMIN 또는 볼륨 생성자만 업로드 가능 (격리 모드 시 워크스페이스 멤버 여부도 확인)
        
 * @summary TUS 업로드 생성
 */
export const createUploadParams = zod.object({
  volumeId: zod.number().describe("볼륨 ID"),
});

export const createUploadHeaderXUploadPathDefault = "/";

export const createUploadHeader = zod.object({
  "X-Upload-Path": zod
    .string()
    .default(createUploadHeaderXUploadPathDefault)
    .describe("업로드 경로 (볼륨 내 저장 위치)"),
});

export const createUploadBody = zod.string();

export const createUploadResponse = zod.string();

/**
 * 
        진행 중인 업로드를 취소하고 임시 파일을 삭제합니다.

        **필수 헤더:**
        - Tus-Resumable: 1.0.0

        **권한:** SUPER_ADMIN 또는 볼륨 생성자만 취소 가능 (격리 모드 시 워크스페이스 멤버 여부도 확인)
        
 * @summary TUS 업로드 취소
 */
export const cancelUploadParams = zod.object({
  volumeId: zod.number().describe("볼륨 ID"),
  uploadId: zod.string().describe("업로드 ID"),
});

export const cancelUploadResponse = zod.string();

/**
 * 
        현재 업로드의 진행 상태를 조회합니다.
        네트워크 중단 후 재개 시 현재 오프셋을 확인하기 위해 사용합니다.

        **필수 헤더:**
        - Tus-Resumable: 1.0.0

        **응답 헤더:**
        - Upload-Offset: 현재까지 업로드된 바이트 수
        - Upload-Length: 전체 파일 크기 (바이트)
        
 * @summary TUS 업로드 상태 조회
 */
export const getUploadStatusParams = zod.object({
  volumeId: zod.number().describe("볼륨 ID"),
  uploadId: zod.string().describe("업로드 ID"),
});

export const getUploadStatusResponse = zod.string();

/**
 * 
        파일 청크를 업로드합니다.
        Upload-Offset 헤더를 통해 이어서 업로드할 위치를 지정합니다.

        **필수 헤더:**
        - Tus-Resumable: 1.0.0
        - Upload-Offset: 현재 업로드 오프셋 (이전 청크의 끝 위치)
        - Content-Type: application/offset+octet-stream

        **요청 바디:**
        - 업로드할 파일 청크 데이터 (바이트 배열)

        **응답 헤더:**
        - Upload-Offset: 업로드 후의 새로운 오프셋 위치

        **업로드 진행률 계산:**
        - 진행률 = (Upload-Offset / Upload-Length) * 100

        **권한:** SUPER_ADMIN 또는 볼륨 생성자만 업로드 가능 (격리 모드 시 워크스페이스 멤버 여부도 확인)
        
 * @summary TUS 청크 업로드
 */
export const uploadChunkParams = zod.object({
  volumeId: zod.number().describe("볼륨 ID"),
  uploadId: zod.string().describe("업로드 ID"),
});

export const uploadChunkBody = zod.string();

export const uploadChunkResponse = zod.string();
