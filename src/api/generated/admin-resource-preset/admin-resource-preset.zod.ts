/**
 * Generated by orval v7.17.2 🍺
 * Do not edit manually.
 * astrago-backend API Documentation
 * Astrago Backend V2 REST API 문서입니다.

## 인증 방법
1. Keycloak을 통해 JWT 토큰을 발급받습니다
2. 우측 상단 'Authorize' 버튼을 클릭합니다
3. JWT 토큰을 입력합니다 (Bearer 접두사 제외)
4. 'Authorize' 버튼을 클릭하여 인증을 완료합니다

## API 설계 원칙
- Hexagonal Architecture 기반 설계
- RESTful API 표준 준수
- HTTP 상태 코드 표준 준수 (201 Created, 204 No Content 등)
- 모든 요청/응답은 JSON 형식 사용

## 응답 형식
모든 API는 다음과 같은 공통 응답 형식을 사용합니다:
```json
{
  "data": { },
  "success": true,
  "message": "요청이 성공적으로 처리되었습니다"
}
```
 * OpenAPI spec version: 2.0.0
 */
import * as zod from "zod";

/**
 * 리소스 프리셋의 상세 정보를 조회합니다. 최신 버전의 프리셋을 반환합니다.
 * @summary 리소스 프리셋 상세 조회
 */
export const getPresetDetailParams = zod.object({
  presetId: zod.number().describe("프리셋 ID"),
});

export const getPresetDetailResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .object({
        presetId: zod.number().describe("프리셋 ID"),
        version: zod.number().describe("버전"),
        presetName: zod.string().describe("프리셋 이름"),
        description: zod.string().optional().describe("설명"),
        resource: zod
          .record(zod.string(), zod.unknown())
          .describe(
            '리소스 정보\n\nGPU 타입별 구조:\n- NORMAL GPU: {"gpuType": "NORMAL", "detail": {"normal": {"requestCount": N}}, "gpuName": "GPU명" (선택)}\n- MIG GPU: {"gpuType": "MIG", "detail": {"mig": [{"profile": "프로파일명", "requestCount": N}]}, "gpuName": null (선택)}\n\n※ gpuName은 모든 GPU 타입에서 선택사항 (null 가능)',
          ),
        jobType: zod.enum(["BATCH", "IDE"]).describe("잡 타입"),
        nodeType: zod.enum(["SINGLE", "MULTI"]).describe("노드 타입"),
        createdBy: zod.string().describe("생성자 ID"),
        createdAt: zod.string().datetime({}).optional().describe("생성 시간"),
      })
      .strict()
      .optional()
      .describe("프리셋 상세 응답"),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 리소스 프리셋을 수정합니다. 수정 시 새로운 버전이 생성됩니다.
 * @summary 리소스 프리셋 수정
 */
export const updatePresetParams = zod.object({
  presetId: zod.number().describe("프리셋 ID"),
});

export const updatePresetBodyPresetNameMin = 0;
export const updatePresetBodyPresetNameMax = 50;

export const updatePresetBodyDescriptionMin = 0;
export const updatePresetBodyDescriptionMax = 2000;

export const updatePresetBody = zod
  .object({
    presetName: zod
      .string()
      .min(updatePresetBodyPresetNameMin)
      .max(updatePresetBodyPresetNameMax)
      .describe("프리셋 이름"),
    description: zod
      .string()
      .min(updatePresetBodyDescriptionMin)
      .max(updatePresetBodyDescriptionMax)
      .optional()
      .describe("프리셋 설명"),
    resource: zod
      .record(zod.string(), zod.unknown())
      .describe(
        '\n            리소스 정보 (cpu, memory, gpu 포함)\n            GPU 타입별 설정:\n                - NORMAL GPU: gpuType="NORMAL", detail.normal.requestCount 설정, gpuName 선택\n                - MIG GPU: gpuType="MIG", detail.mig 배열에 profile 1개와 requestCount 설정 (다중 프로파일 불가), gpuName 선택\n                - GPU 미사용: gpu 필드 생략\n            ※ gpuName은 모든 GPU 타입에서 선택사항 (자동 스케줄링 지원)\n        ',
      ),
    jobType: zod.enum(["BATCH", "IDE"]).describe("잡 타입 (BATCH, IDE)"),
    nodeType: zod
      .enum(["SINGLE", "MULTI"])
      .describe("노드 타입 (SINGLE, MULTI)"),
  })
  .strict()
  .describe("프리셋 수정 요청");

export const updatePresetResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .object({
        presetId: zod.number().describe("프리셋 ID"),
        version: zod.number().describe("버전"),
        presetName: zod.string().describe("프리셋 이름"),
        description: zod.string().optional().describe("설명"),
        resource: zod
          .record(zod.string(), zod.unknown())
          .describe(
            '리소스 정보\n\nGPU 타입별 구조:\n- NORMAL GPU: {"gpuType": "NORMAL", "detail": {"normal": {"requestCount": N}}, "gpuName": "GPU명" (선택)}\n- MIG GPU: {"gpuType": "MIG", "detail": {"mig": [{"profile": "프로파일명", "requestCount": N}]}, "gpuName": null (선택)}\n\n※ gpuName은 모든 GPU 타입에서 선택사항 (null 가능)',
          ),
        jobType: zod.enum(["BATCH", "IDE"]).describe("잡 타입"),
        nodeType: zod.enum(["SINGLE", "MULTI"]).describe("노드 타입"),
        createdBy: zod.string().describe("생성자 ID"),
        createdAt: zod.string().datetime({}).optional().describe("생성 시간"),
      })
      .strict()
      .optional()
      .describe("프리셋 상세 응답"),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 리소스 프리셋을 삭제합니다 (soft delete). 해당 프리셋의 모든 버전이 삭제됩니다.
 * @summary 리소스 프리셋 삭제
 */
export const deletePresetParams = zod.object({
  presetId: zod.number().describe("삭제할 프리셋 ID"),
});

/**
 * 등록된 리소스 프리셋 목록을 페이징하여 조회합니다. 잡 타입과 노드 타입으로 필터링할 수 있습니다.
 * @summary 리소스 프리셋 목록 조회
 */
export const getPresetsQueryPageableRequestPageSizeMax = 100;

export const getPresetsQueryParams = zod.object({
  jobType: zod
    .enum(["BATCH", "IDE"])
    .optional()
    .describe("잡 타입 필터 (BATCH, IDE)"),
  nodeType: zod
    .enum(["SINGLE", "MULTI"])
    .optional()
    .describe("노드 타입 필터 (SINGLE, MULTI)"),
  pageableRequest: zod.object({
    pageNo: zod.number().describe("페이지 번호 (0부터 시작)"),
    pageSize: zod
      .number()
      .min(1)
      .max(getPresetsQueryPageableRequestPageSizeMax)
      .describe("페이지 크기"),
  }),
});

export const getPresetsResponse = zod
  .object({
    status: zod.enum(["SUCCESS", "FAIL", "ERROR"]),
    errorCode: zod.string().optional(),
    data: zod
      .object({
        totalSize: zod.number(),
        totalPageNum: zod.number(),
        currentPageNo: zod.number(),
        content: zod.array(
          zod
            .object({
              presetId: zod.number().describe("프리셋 ID"),
              version: zod.number().describe("버전"),
              presetName: zod.string().describe("프리셋 이름"),
              description: zod.string().optional().describe("설명"),
              resource: zod
                .record(zod.string(), zod.unknown())
                .describe(
                  '리소스 정보\n\nGPU 타입별 구조:\n- NORMAL GPU: {"gpuType": "NORMAL", "detail": {"normal": {"requestCount": N}}, "gpuName": "GPU명" (선택)}\n- MIG GPU: {"gpuType": "MIG", "detail": {"mig": [{"profile": "프로파일명", "requestCount": N}]}, "gpuName": null (선택)}\n\n※ gpuName은 모든 GPU 타입에서 선택사항 (null 가능)',
                ),
              jobType: zod.enum(["BATCH", "IDE"]).describe("잡 타입"),
              nodeType: zod.enum(["SINGLE", "MULTI"]).describe("노드 타입"),
              createdBy: zod.string().describe("생성자 ID"),
              createdAt: zod
                .string()
                .datetime({})
                .optional()
                .describe("생성 시간"),
            })
            .strict()
            .describe("프리셋 상세 응답"),
        ),
      })
      .strict()
      .optional(),
    message: zod.string().optional(),
    timestamp: zod.number(),
  })
  .strict();

/**
 * 새로운 리소스 프리셋을 생성합니다. CPU, 메모리, GPU 등의 리소스 구성을 템플릿으로 저장합니다.
 * @summary 리소스 프리셋 생성
 */
export const createPresetBodyPresetNameMin = 0;
export const createPresetBodyPresetNameMax = 50;

export const createPresetBodyDescriptionMin = 0;
export const createPresetBodyDescriptionMax = 2000;

export const createPresetBody = zod
  .object({
    presetName: zod
      .string()
      .min(createPresetBodyPresetNameMin)
      .max(createPresetBodyPresetNameMax)
      .describe("프리셋 이름"),
    description: zod
      .string()
      .min(createPresetBodyDescriptionMin)
      .max(createPresetBodyDescriptionMax)
      .optional()
      .describe("프리셋 설명"),
    resource: zod
      .record(zod.string(), zod.unknown())
      .describe(
        '\n            리소스 정보 (cpu, memory, gpu 포함)\n            GPU 타입별 설정:\n                - NORMAL GPU: gpuType="NORMAL", detail.normal.requestCount 설정, gpuName 선택\n                - MIG GPU: gpuType="MIG", detail.mig 배열에 profile 1개와 requestCount 설정 (다중 프로파일 불가), gpuName 선택\n                - GPU 미사용: gpu 필드 생략\n            ※ gpuName은 모든 GPU 타입에서 선택사항 (자동 스케줄링 지원)\n        ',
      ),
    jobType: zod.enum(["BATCH", "IDE"]).describe("잡 타입 (BATCH, IDE)"),
    nodeType: zod
      .enum(["SINGLE", "MULTI"])
      .describe("노드 타입 (SINGLE, MULTI)"),
  })
  .strict()
  .describe("프리셋 생성 요청");
