import { mpsInfoSchema } from "@/domain/node/schemas/mps.schema";
import { makeMock } from "@/shared/utils/mock.util";

/**
 * MPS 설정 정보 모킹 데이터
 */
export const mpsInfoMock = makeMock(mpsInfoSchema);
