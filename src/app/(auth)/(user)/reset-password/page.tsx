import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "비밀번호 재설정",
};

/**
 * 비밀번호 재설정 페이지
 * TODO: ResetPasswordMain 컴포넌트 구현 필요
 */
export default function ResetPasswordPage() {
  return <div>비밀번호 재설정 페이지</div>;
}
