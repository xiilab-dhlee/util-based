import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Workspace Management",
};

export default function AdminWorkspaceRequestHistoryPage() {
  return <div>AdminWorkspaceRequestHistoryPage</div>;
}
