import { atom } from "jotai";

/** YAML 로그 모달 표시 여부 */
export const openYamlLogModalAtom = atom<boolean>(false);

/** 사용자 워크스페이스 모달 표시 여부 */
export const openUserWorkspaceModalAtom = atom<boolean>(false);
